<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
</head>
<body>
<% include navbar.ejs %>
<div class="homepage_content">
    <h1>Dodaj przetarg</h1>
    <div style="width: 60%; padding-top: 20px">
        <form method="post" action="/przetargi/dodaj/">
            <label for="title" >
                <span>Przedmiot przetargu:</span>
                <input type="text" id="title" name="title" placeholder="Nazwa" required/>
            </label><br>
            <label for="contracting_authority">
                <span>Instytucja zamawiająca:</span>
                <input type="text" id="contracting_authority" name="contracting_authority" placeholder="Nazwa" required/>
            </label><br>
            <label for="start_date">
                <span>Data rozpoczęcia:</span>
                <input type="datetime-local" id="start_date" name="start_date" required>
            </label><br>
            <label for="end_date">
                <span>Data zakończenia:</span>
                <input type="datetime-local" id="end_date" name="end_date" required>
            </label>
            <% if(info.timeError){%>
                <p style="text-align: center; color: red; font-weight: bold"><%= info.timeError %></p>
            <% }%><br>
            <label for="description">
                <span>Opis:</span>
                <textarea id="description" name="description" placeholder="Opis" rows="3" required></textarea>
            </label><br>
            <label for="max_value">
                <span>Maksymalna wartość:</span>
                <input type="number" id="max_value" name="max_value">
            </label>
            <% if(info.valueError){%>
                <p style="text-align: center; color: red; font-weight: bold"><%= info.valueError %></p>
            <% }%><br>
            <input class="submit_button" type="submit" value="Utwórz przetarg">
            <% if(info.success){ %>
                <p style="text-align: center; color: green; font-weight: bold">Pomyślnie utworzono nowy przetarg</p>
            <% } %>
        </form>
    </div>
</div>
</body>
</html>
